.main_chat__list__post
  .main_chat__list__post__top
    .main_chat__list__post__top__name
      = message.user.name
    .main_chat__list__post__top__date
      = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
  .main_chat__list__post__comment
    - if message.content.present?
      %p.main_chat__list__post__comment__content
        = message.content
    = image_tag message.image.url, class: 'main_chat__list__post__comment__image' if message.image.present?